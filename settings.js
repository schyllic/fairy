// Observer location — used for twilight, planet visibility, and constellation calculations.
const OBSERVER = {
  lat:  37.0,   // degrees North
  lon: -80.0,   // degrees East (negative = West)
  tz:  'America/New_York',  // IANA timezone name (used for display only)
};
